/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/huedoku",
    output: "export",
    images: {
        unoptimized: true
    },
    reactStrictMode: true,
};

export default nextConfig;
